## 20260215_171244

`barrel.md`

> I want to build a markdown viewer and editor. Something extremely performant and uses minimal resources. Something Carmac would be proud of. I can limit scope to supporting macOS only. Let’s think through this and create a plan.
> 
>   
> 
> Teammate 1 Brainstorm:
> 
>   
> 
> This is a great project, Tom. Let me think through this properly — a Carmack-worthy markdown editor means obsessing over the right architectural choices before writing a single line of code.
> 
> Here’s how I’d approach the design:
> 
> Core Philosophy
> 
> Carmack’s approach boils down to: do the simplest thing that could work, measure everything, eliminate waste. For a markdown editor that means minimizing layers of abstraction, avoiding frameworks that hide allocation patterns, and keeping the hot path (keystroke → re-parse → re-render) as tight as possible.
> 
> Key Architectural Decisions
> 
> Language: Swift + AppKit (not SwiftUI)
> 
> SwiftUI’s declarative diffing is antithetical to the goal here. AppKit gives you direct control over the text system, and NSTextView is battle-tested for text editing. Swift gives you value types to avoid heap allocations on the hot path and easy interop with C libraries. No Electron, no WebView for rendering.
> 
> Parsing: cmark-gfm (C library) with incremental strategy
> 
> Don’t write your own parser — cmark is a state machine in C, it’s extremely fast, and it’s CommonMark-compliant. The real performance trick is incremental parsing: on each edit, only re-parse the affected block-level node (paragraph, list, code block), not the entire document. cmark’s AST makes this feasible — you track which byte ranges map to which AST nodes, and on edit you invalidate/re-parse only the dirty range plus surrounding context.
> 
> Text System: Custom NSTextStorage subclass
> 
> This is where the Carmack energy lives. NSTextStorage is the backbone of Cocoa’s text system. By subclassing it, you intercept every edit and can:
> 
> ∙ Trigger incremental re-parse of only the changed region
> 
> ∙ Apply syntax highlighting as attributed string changes in-place (no full re-render)
> 
> ∙ Keep a parallel data structure mapping document ranges → cmark AST nodes
> 
> Rendering: NSTextView + NSLayoutManager (TextKit 1, not TextKit 2)
> 
> TextKit 2 is still buggy and less predictable. TextKit 1’s NSLayoutManager gives you fine-grained control over glyph generation and layout. For the preview pane (if you want one), render to NSAttributedString from the cmark AST — no WebView.
> 
> File I/O: Memory-mapped files
> 
> Use mmap for large files. No reason to read an entire 10MB markdown file into a heap buffer. The OS virtual memory system handles paging for free. For saves, use atomic writes (Data.write(to:options:.atomic)).
> 
> Architecture Sketch
> 
>   
> 
> Keystroke
> 
>   → NSTextStorage.edited() (captures edit range + delta)
> 
>   → Incremental block invalidation (find affected AST node)
> 
>   → cmark re-parse of dirty block only
> 
>   → AST diff (old node vs new node)
> 
>   → Targeted attribute updates (syntax highlighting)
> 
>   → NSLayoutManager invalidates only affected glyph range
> 
>   →
> 
>   
> 
>   
> 
> The goal: a keystroke-to-
> 
> latency under 1ms for documents up to 100K lines.
> 
> Component Breakdown
> 
> 1. Document Model — A rope or gap buffer backing the raw text. Swift’s String is fine for moderate files, but for truly large files a rope data structure (tree of string chunks) gives you O(log n) insertions. Start with String, profile, upgrade if needed.
> 
> 2. Block Map — An interval tree or sorted array mapping byte ranges to cmark AST block nodes. On edit, binary search to find affected blocks, mark dirty, re-parse only those.
> 
> 3. Syntax Highlighter — Walks the cmark AST and maps node types to NSAttributedString attributes. Operates only on dirty ranges. No regex-based highlighting — the AST already tells you what everything is.
> 
> 4. Preview Renderer (optional) — If you want a side-by-side preview, render from cmark AST → NSAttributedString in a second NSTextView (read-only). Not a WebView. This shares the same AST, so no double-parsing.
> 
> 5. File Watcher — Use DispatchSource.makeFileSystemObjectSource for external change detection. Important for Obsidian-style vault workflows where other tools might modify the file.
> 
> What to Skip (Scope Limits)
> 
> For v1, skip: extensions beyond GFM, collaborative editing, plugin system, custom themes (hardcode a good dark/light pair), image preview inline (just show the markdown syntax), and any kind of document tabs/project management. One file, one window, blazing fast.
> 
> Build Plan
> 
>   
> 
>   
> 
>   
> 
> |Phase|Deliverable                                    |Focus                                                      |
> 
> |-----|-----------------------------------------------|-----------------------------------------------------------|
> 
> |0    |Swift package, cmark-gfm linked via C interop  |Build system, prove cmark works from Swift                 |
> 
> |1    |Custom NSTextStorage + basic NSTextView        |Raw editing, no highlighting yet, measure keystroke latency|
> 
> |2    |Full-document cmark parse → syntax highlighting|Get the AST → attributes pipeline working                  |
> 
> |3    |Incremental parsing (block invalidation)       |Only re-parse dirty blocks on edit                         |
> 
> |4    |Memory-mapped file I/O + large file testing    |Profile with 1MB+ markdown files                           |
> 
> |5    |Preview pane (AST → attributed string)         |Side-by-side, sharing the same AST                         |
> 
> |6    |Polish: find/replace, line numbers, minimap    |Quality of life                                            |
> 
>   
> 
> Benchmarking from Day 1
> 
> Instrument from the very first commit. Key metrics to track on every change: keystroke-to-
> 
> latency (target <1ms), full-document parse time, memory footprint per 1K lines, and time-to-first-
> 
> on cold open. Use os_signpost for Instruments integration — it’s zero-cost when not profiling.
> 
>   
> 
> Want to start with Phase 0 — getting the Swift package scaffolded with cmark-gfm linked? Or do you want to debate any of these choices first? I’m particularly open to discussing whether a rope data structure is worth the complexity upfront vs. starting with String.
> 
>   
> 
> Teammate 2 Brainstorm:
> 
>   
> 
> Here’s a plan for a macOS-only Markdown editor and viewer that is fast, small, and boring in the best way.
> 
>   
> 
> Product shape
> 
>   
> 
> Two panes, one engine.
> 
> • Editor: plain text, blazing fast, huge files ok
> 
> • Preview: near-instant, incremental updates
> 
> • Optional: single-pane “hybrid” mode (rendered Markdown with editable source underneath)
> 
> • Scope guardrails: CommonMark + a small, explicit extension set you control
> 
>   
> 
> The main trick is: treat Markdown as a structured document with incremental parsing and incremental rendering, not “string in, HTML out” on every keystroke.
> 
>   
> 
> Hard constraints for performance
> 
> • No WebView for preview (it is convenient but heavy and unpredictable)
> 
> • No “rebuild attributed string for whole document” on each edit
> 
> • No “full reparse” on each edit
> 
> • Avoid large allocations and copying, especially with big files
> 
>   
> 
> Architecture overview
> 
>   
> 
> 6) Document model (text storage)
> 
>   
> 
> You want a text storage that supports:
> 
> • O(1) or log-time inserts/deletes
> 
> • cheap slicing by range
> 
> • stable position mapping across edits
> 
>   
> 
> Good options:
> 
> • Rope / piece table / gap buffer style storage
> 
> • If you stay fully in Swift/AppKit, you can still wrap the system text engine but keep your own indexing layer.
> 
>   
> 
> Pragmatic macOS path:
> 
> • Use TextKit 2 for editing UI, but keep your own lightweight “document snapshot” abstraction for parsing and preview.
> 
> • Maintain a line index (newline offsets) for fast range to line/column mapping.
> 
>   
> 
> 7) Incremental parse tree
> 
>   
> 
> Use an incremental parser that can update only the affected region after an edit.
> 
>   
> 
> Two solid approaches:
> 
> • Tree-sitter Markdown: battle-tested incremental parsing, produces a concrete syntax tree quickly.
> 
> • Custom incremental Markdown parser: smaller dependency surface, but more work and easy to get wrong.
> 
>   
> 
> If “Carmack proud” means minimal surprises and measurable performance, Tree-sitter is a great core. You can wrap it so the rest of your app never depends on its details.
> 
>   
> 
> Output of this stage:
> 
> • An AST or CST with node ranges in the source buffer.
> 
>   
> 
> 8) Render pipeline (preview)
> 
>   
> 
> Render to a lightweight “layout tree” that is optimized for drawing.
> 
> • Convert Markdown nodes to blocks: headings, paragraphs, lists, code blocks, blockquotes, tables (if supported)
> 
> • Each block produces runs: text spans with style and links, plus inline elements (code spans, emphasis)
> 
>   
> 
> Do incremental work:
> 
> • Only rebuild blocks whose source ranges intersect the edit invalidation range.
> 
> • Cache block layouts (line breaks) and reuse if their text and style are unchanged.
> 
>   
> 
> 9) Drawing
> 
>   
> 
> Avoid HTML. Draw with native text.
> 
> • Use CoreText or TextKit layout objects for preview blocks
> 
> • Use tiled rendering: only draw blocks visible in the scroll viewport
> 
> • Precompute block heights and maintain a prefix-sum index so scrolling is cheap
> 
>   
> 
> This gets you low memory, high FPS scrolling, and predictable CPU.
> 
>   
> 
> 10) Sync editor and preview
> 
>   
> 
> Make positions first-class:
> 
> • Maintain mapping between editor selection and parse node range
> 
> • Allow click in preview to move cursor in source
> 
> • Keep this mapping cheap using your line index + node ranges
> 
>   
> 
> Scope definition (what Markdown features)
> 
>   
> 
> Start narrow and explicit. Example v1:
> 
> • Paragraphs, headings, emphasis/strong, inline code
> 
> • Links, images (images can be “lazy loaded”)
> 
> • Fenced code blocks with language tag (highlighting optional at first)
> 
> • Lists (ordered/unordered), blockquotes
> 
> • Horizontal rule
> 
> • No tables at first unless you really want them, tables are complexity magnets
> 
>   
> 
> Then decide on extensions:
> 
> • GitHub style task lists
> 
> • Strikethrough
> 
> • Tables
> 
> • Footnotes
> 
>   
> 
> Tech stack options
> 
>   
> 
> Option A: Swift + AppKit, Tree-sitter core
> 
> • UI: AppKit (NSTextView via TextKit 2, custom NSView for preview)
> 
> • Parser: tree-sitter-markdown (C) via Swift package wrapper
> 
> • Rendering: CoreText
> 
>   
> 
> Pros: native feel, excellent macOS integration, fast enough for huge docs
> 
> Cons: bridging and careful memory management, but manageable
> 
>   
> 
> Option B: Rust core engine + Swift UI
> 
> • Rust crate: text storage + incremental parse + layout model
> 
> • Swift: UI only
> 
>   
> 
> Pros: maximum control, easy to benchmark, very Carmack-ish separation
> 
> Cons: more engineering, FFI design, debugging across boundary
> 
>   
> 
> If your goal is “minimal resources” plus future portability, Option B is sweet. If your goal is shipping quickly on macOS, Option A is the fastest path without losing performance.
> 
>   
> 
> Performance targets (make them real)
> 
>   
> 
> Pick targets you can benchmark from day one:
> 
> • Open 50 MB markdown file: < 1s to first paint
> 
> • Keystroke-to-preview update: p95 < 16 ms for typical edits, p99 < 50 ms
> 
> • Scroll preview at 120 Hz on a large doc: no dropped frames
> 
> • Memory: baseline app < 150 MB, large file < 400 MB (tune to your tolerance)
> 
>   
> 
> Add a built-in “perf HUD”:
> 
> • parse time, layout time, draw time
> 
> • dirty region size
> 
> • number of blocks re-laid out
> 
>   
> 
> Build plan (phased)
> 
>   
> 
> Phase 0: Spike and measurement (1 to 2 days of effort)
> 
> • Basic window with editor and preview
> 
> • Load file into editor
> 
> • Render preview as plain text blocks (no styling) with incremental invalidation placeholder
> 
> • Add perf HUD + timing hooks
> 
>   
> 
> Deliverable: you can measure everything before building “the real thing”.
> 
>   
> 
> Phase 1: Incremental parsing
> 
> • Integrate tree-sitter
> 
> • On edit, compute invalidation range, reparse incrementally
> 
> • Build a minimal block list from parse tree: headings, paragraphs, code blocks, lists
> 
> • Unit tests for range mapping and incremental updates
> 
>   
> 
> Deliverable: parse tree updates fast and correctly.
> 
>   
> 
> Phase 2: Incremental layout and native rendering
> 
> • Convert blocks to styled text runs
> 
> • Layout per block using CoreText, cache results
> 
> • Tiled preview rendering and viewport-only drawing
> 
> • Scroll performance tuned and stable
> 
>   
> 
> Deliverable: preview feels instant and scrolls smoothly.
> 
>   
> 
> Phase 3: Editor polish and sync features
> 
> • Click preview to jump cursor in editor
> 
> • Keep selection highlighting in preview
> 
> • Search, outline sidebar from headings (cheap, uses parse tree)
> 
> • File watching and external change merge strategy (optional)
> 
>   
> 
> Deliverable: it becomes a real editor, not a demo.
> 
>   
> 
> Phase 4: Features that tend to blow up complexity
> 
>   
> 
> Pick carefully:
> 
> • syntax highlighting for code blocks (incremental, per visible block)
> 
> • images (lazy decode, cache, downsample)
> 
> • tables (only if you really need them)
> 
> • export (PDF, HTML) using a separate pipeline so it doesn’t pollute your live renderer
> 
>   
> 
> Key implementation details to get right
> 
> • Edit invalidation: given an edit at range R, expand to safe reparse region (often nearest blank line boundaries, plus fence boundaries) to avoid pathological reparses.
> 
> • Stable IDs for blocks: block identity should persist across edits so caches are reusable.
> 
> • Avoid pathological allocations: prefer slices/views into the rope, and small structs.
> 
> • No global relayout: enforce this with counters in the perf HUD.
> 
> • Cancelation: if user types fast, cancel in-flight preview updates and only render latest snapshot.
> 
>   
> 
> A concrete “v1 spec” you can paste into a repo
> 
>   
> 
> If you want, I can write an initial SPEC.md and ARCHITECTURE.md with:
> 
> • module boundaries (Text, Parse, Layout, Render, UI)
> 
> • data structures (rope/piece table, line index, block cache)
> 
> • benchmark harness plan
> 
> • feature flags and scope rules
> 
>   
> 
> And if you tell me which direction you prefer (Swift-only vs Rust-core), I’ll tailor the module plan and the first milestone checklist to match.


Below is a concrete plan that stays macOS-only, keeps the hot path tight, and gives you room to scale from “works” to “absurdly fast” without rewriting everything.

  

  

What you are building

  

  

A native macOS Markdown editor with a live preview that stays responsive under big files and high typing rates.

  

Core behaviors:

  

- Fast source editing (never block the main thread on parsing or preview work)
- Live preview that updates incrementally (do as little work as possible per edit)
- Minimal memory churn (avoid rebuilding whole strings, whole attributed strings, or whole layouts)

  

  

Scope guardrails for v1:

  

- Single file, single window
- CommonMark plus a small explicit extension set (pick a short list and treat everything else as plain text)
- No WebView, no HTML render loop
- No plugin system, no “workspace” or vault management

  

  

  

Architectural principles

  

  

Non-negotiable invariants:

  

1. Keystroke path stays lean: editing must remain smooth even if preview lags.
2. Incremental by default: edits only invalidate a small region, and only that region gets reparsed and rerendered.
3. Cache everything with stable IDs: blocks keep identities across edits so you reuse layout and rendering.
4. Measure from day 1: you cannot optimize what you do not track.

  

  

  

Recommended stack

  

  

  

UI

  

  

- AppKit
- Editor: NSTextView with minimal attributes (keep source view mostly plain).
- Preview: start with a second read-only text view for speed of implementation, then graduate to a custom tiled CoreText view if needed.

  

  

Text system choice:

  

- Start with TextKit 1 (NSTextView + NSLayoutManager) for predictability and fine control over invalidation.
- Keep your design modular so you can experiment later without touching the core engine.

  

  

  

Parsing

  

  

Two viable strategies:

  

Path A (pragmatic, small dependency surface): cmark-gfm

  

- Pros: correctness, CommonMark/GFM semantics, fast C parser, stable.
- Cons: not inherently incremental, you implement incremental behavior by reparsing only a safe region.

  

  

Path B (incremental parsing out of the box): tree-sitter Markdown

  

- Pros: true incremental parse, gives you structural ranges cheaply.
- Cons: introduces a grammar dependency and you still need a semantic layer for rendering choices.

  

  

If you want the smallest moving parts first, go with cmark-gfm and implement “incremental enough” by region invalidation. If you strongly care about deep incremental structure from the start, go tree-sitter.

  

My suggestion:

  

- Start with cmark-gfm for correctness and early shipping.
- Design the core so the parser is a pluggable module. If later you hit a wall, you can swap in tree-sitter for structure while keeping the rest intact.

  

  

  

Core modules and data structures

  

  

Design this as a small engine with clean boundaries:

  

  

1) DocumentSnapshot

  

  

An immutable view used by background work.

  

- text: NSString (source of truth for v1)
- revision: Int (monotonic generation)
- lineIndex: LineIndex (newline offsets in UTF-16 units)

  

  

Why NSString and UTF-16 offsets:

  

- AppKit uses NSRange in UTF-16.
- Keeping offsets in the same coordinate system avoids constant expensive mapping.

  

  

  

2) LineIndex

  

  

Maintains line start offsets to make these operations fast:

  

- Expand an edit to whole lines
- Map “line, column” style positions if needed
- Find block boundaries by scanning lines near the edit

  

  

Implementation shape:

  

- [Int] lineStarts where each entry is a UTF-16 offset
- Incrementally update only the affected line range on edit (do not rescan the whole file)

  

  

  

3) DirtyTracker

  

  

Coalesces edits into a minimal set of invalid ranges:

  

- Merges overlapping and adjacent ranges
- Can grow a range to “safe reparse region” (more on that below)

  

  

  

4) BlockIndex

  

  

A flat array of block records, ordered by source range:

  

BlockRecord

  

- id: BlockID (stable identity)
- range: NSRange (source range in UTF-16)
- kind: BlockKind (heading, paragraph, list, code fence, quote, etc.)
- hash: UInt64 (hash of source slice + relevant style settings)
- layoutKey: LayoutKey (font size, width constraints, theme variant)

  

  

Stable IDs matter because they let you reuse cached layout even when offsets shift.

  

How to keep IDs stable:

  

- For unchanged blocks, preserve ID.
- For changed region, create new IDs only for blocks that truly changed content or kind.
- For moves caused by insertions above, keep IDs and just update ranges.

  

  

  

5) RenderCache

  

  

Caches per-block layout and draw artifacts:

  

- For NSTextView preview: cached NSAttributedString per block (or per paragraph).
- For CoreText preview: cached CTFramesetter or precomputed line breaks, plus measured height.

  

  

  

6) PreviewModel

  

  

A lightweight representation of what the preview needs:

  

- List of blocks with their attributed runs or layout handles
- Heights and y-offsets (prefix sums) for fast scrolling if using custom view

  

  

  

The edit to preview pipeline

  

  

  

Hot path (main thread)

  

  

On every edit:

  

1. NSTextStorage.processEditing() (or delegate callback)
2. Record editedRange and changeInLength into DirtyTracker
3. Update LineIndex incrementally for affected lines
4. Schedule background update with cancellation

  

  

Main thread must not:

  

- Parse Markdown
- Build large attributed strings
- Perform layout for the whole preview

  

  

  

Background path (coalesced, cancelable)

  

  

A worker job does:

  

1. Capture a DocumentSnapshot (text + revision + line index)
2. Compute safe reparse region from the dirty range
3. Parse only that region (or a slightly expanded region)
4. Build new blocks for that region and diff against existing BlockIndex
5. Build updated preview artifacts only for changed blocks
6. Dispatch a small “apply diff” back to the main thread, guarded by revision

  

  

  

Safe reparse region

  

  

Markdown is context sensitive around lists, indentation, and fenced blocks. The trick is to expand the dirty range to a region that is guaranteed to reparse correctly without reparsing the whole file.

  

A practical rule set that works well:

  

- Expand to full lines for the edit.
- Then expand upward and downward until you hit a “hard boundary”.
- Hard boundaries usually include:  
    

- Blank line that is not indented as part of a list/quote continuation
- Start or end of a fenced code block
- Heading line
- Horizontal rule line

-   
    
- Add a small safety margin of a few lines above and below.
- If your scan detects you are inside a fence, expand to the entire fence.

  

  

This is deliberately simple and measurable. You can tighten it later with profiling.

  

  

Preview rendering strategy

  

  

  

Stage 1 (fast to build): preview as read-only NSTextView

  

  

- Build attributed strings per changed block and splice them into the preview text storage.
- Do not rebuild preview for the entire document.
- Use minimal attributes:  
    

- Fonts and weights for headings
- Monospace for code
- Link attributes
- Paragraph styles for list indentation

-   
    

  

  

This will get you a working, native preview quickly and is surprisingly performant if you keep updates localized.

  

  

Stage 2 (max performance): custom preview view with CoreText and tiling

  

  

Use a custom NSView inside an NSScrollView:

  

- Preview is a list of blocks, each block can layout itself given a width.
- Only layout and draw blocks that are visible.
- Cache layout results keyed by (BlockID, width, theme).

  

  

This gives you:

  

- Very low memory overhead for huge docs
- Predictable scrolling performance
- No hidden relayout costs from TextKit

  

  

Graduation criteria:

  

- If preview NSTextView starts dropping frames while scrolling large files
- If attributed string memory becomes dominant
- If you need strict control over what gets laid out and when

  

  

  

File I/O and large file behavior

  

  

Open:

  

- For v1: Data(contentsOf:) plus decode to String is acceptable up to moderately large files.
- For large file mode: memory-map (mmap) and decode once.
- Build LineIndex in one pass.

  

  

Save:

  

- Atomic writes (write to temp, then replace).
- Avoid converting the whole document multiple times. Treat “export” and “save” as separate pipelines.

  

  

File watching:

  

- Use a file descriptor plus DispatchSource to detect external modifications.
- If file changed externally:  
    

- If your buffer is unmodified, reload
- If modified, show a minimal conflict prompt and offer: overwrite, reload, or diff (diff can be a later feature)

-   
    

  

  

  

Performance targets and instrumentation

  

  

Make targets you can test continuously:

  

Typing:

  

- Main thread time per edit (excluding preview apply): aim for under 1 ms typical
- Preview update latency:  
    

- p50 under 16 ms for small edits
- p95 under 50 ms for medium docs
- Large docs degrade gracefully, never blocking typing

-   
    

  

  

Open:

  

- Time to first paint: under 300 ms for small files, under 1 s for large files

  

  

Memory:

  

- Baseline low tens of MB
- Growth roughly proportional to visible preview and block cache, not proportional to full document size

  

  

Instrumentation:

  

- Use os_signpost spans for:  
    

- Edit capture
- Dirty range computation
- Reparse region scan
- Parse time
- Block diff time
- Preview artifact build time
- Apply diff time on main thread

-   
    
- Add a small optional in-app HUD:  
    

- revision number
- dirty range size
- reparse region size
- parse time, layout time, apply time
- number of blocks changed

-   
    

  

  

  

Concrete phased build plan

  

  

  

Phase 0: Skeleton + measurement

  

  

Deliverable:

  

- Single window with split view
- Left: editable NSTextView
- Right: placeholder preview view
- Open and save a file
- Perf hooks and basic HUD scaffolding

  

  

Success criteria:

  

- You can type without stutter in a 1 MB file even before preview exists.

  

  

  

Phase 1: Full parse preview, coalesced

  

  

Deliverable:

  

- Integrate cmark-gfm
- On edit, debounce and parse full document in background
- Render preview as attributed string (quick and correct)

  

  

Success criteria:

  

- Correct rendering for supported features
- No main thread stalls
- You have baseline parse and render timings

  

  

This phase is intentionally not incremental yet. It establishes correctness and a benchmark baseline.

  

  

Phase 2: Incremental invalidation + block index

  

  

Deliverable:

  

- Implement LineIndex and DirtyTracker properly
- Implement safe reparse region expansion
- Maintain BlockIndex
- Reparse only the reparse region and update only affected preview blocks

  

  

Success criteria:

  

- Typical typing updates a small region only
- Preview updates stay fast as files grow

  

  

  

Phase 3: Cache and diff hardening

  

  

Deliverable:

  

- Stable BlockID behavior
- Per-block hash to avoid redundant rebuilds
- Preview cache reuse
- Cancelation: if a new edit arrives, older background work is discarded

  

  

Success criteria:

  

- Rapid typing does not queue work
- CPU stays low during continuous typing

  

  

  

Phase 4: Large file mode + I/O

  

  

Deliverable:

  

- mmap open path for big files
- Atomic save
- External change detection
- Stress tests with 10 MB to 50 MB markdown

  

  

Success criteria:

  

- Open is fast, memory stable, typing remains responsive

  

  

  

Phase 5: Optional custom preview renderer

  

  

Deliverable:

  

- Replace preview NSTextView with a tiled CoreText view if needed
- Block height caching and viewport-only drawing

  

  

Success criteria:

  

- Scrolling stays smooth on huge docs
- Preview memory footprint drops

  

  

  

Phase 6: Editor quality of life

  

  

Pick features that do not threaten the hot path:

  

- Find/replace
- Outline sidebar from headings (free from parse tree)
- Click in preview to jump to source location
- Basic theming (two themes only)
- Export pipeline (HTML/PDF) separate from live preview engine

  

  

  

Testing strategy

  

  

Correctness:

  

- Golden files: markdown input + expected high-level block structure and some text attributes
- Edge cases: fences, nested lists, mixed indentation, long paragraphs

  

  

Incremental robustness:

  

- Random edit fuzzing:  
    

- Start from a document
- Apply random inserts/deletes
- Compare incremental result to a full reparse result (for supported features)

-   
    
- Range mapping tests: ensure preview clicks map back to the correct source ranges

  

  

Performance regression:

  

- Benchmark harness that opens:  
    

- Small (50 KB), medium (1 MB), large (10 MB, 50 MB)

-   
    
- Scripted typing simulation to produce comparable metrics over time

  

  

  

A practical “v1 feature set” to lock in

  

  

Block:

  

- headings, paragraphs
- fenced code blocks
- quotes
- ordered and unordered lists (basic)
- horizontal rule

  

  

Inline:

  

- emphasis, strong
- inline code
- links

  

  

Explicitly not in v1:

  

- tables
- footnotes
- embedded images rendered inline (you can show placeholders)

  

  

  

  

If you want a strong starting decision: build Phases 0 to 2 with AppKit, cmark-gfm, preview as a read-only text view, and an incremental region strategy. That gets you something usable quickly and gives you real measurements to justify any deeper renderer work later. The moment metrics show TextKit preview becoming the bottleneck, switch only the preview side to a custom CoreText view while keeping the parser, block index, and diff machinery unchanged.


> Propose a layout architecture we can use to seed this project. This will be built agentically so we also need to optimize for the following:
> 
>   
> 
> https://cursor.com/blog/agent-best-practices
> 
>   
> 
> https://cursor.com/blog/scaling-agents
> 
>   
> 
> https://banay.me/dont-waste-your-backpressure/
> 
>   
> 
> https://www.humanlayer.dev/blog/writing-a-good-claude-md


Here’s a seedable layout architecture that optimizes for extreme performance and for agentic development. It’s two things at once:

1. A runtime layout pipeline (parse → blocks → layout → draw) that is incremental, cacheable, and cancelable

2. A repo layout that gives agents strong backpressure, clear boundaries, and progressive disclosure

  

⸻

  

Layout engine architecture

  

The core idea

  

Treat Markdown preview as a list of independent blocks (paragraphs, headings, list items, code fences, quotes). Each block is:

• Identified by a stable BlockID

• Has a source NSRange in UTF-16 (to match AppKit)

• Can be laid out independently given a width constraint

• Can be cached independently

  

Then the preview view draws only the visible blocks and re-lays out only blocks affected by an edit.

  

This avoids the classic traps:

• Rebuilding a whole attributed string for the document

• Re-laying out everything on every keystroke

• Any HTML/WebView pipeline

  

Data model (minimal and explicit)

  

Parse output (semantic-ish)

• BlockModel

• id: BlockID

• kind: BlockKind (heading, paragraph, listItem, codeFence, quote, thematicBreak…)

• range: NSRange (source)

• inlines: [InlineSpan] (for paragraph/heading/list items)

• metadata: BlockMetadata (heading level, fence language tag, list depth, quote depth…)

  

Layout output (view-independent)

• LayoutBlock

• id: BlockID

• measuredHeight: CGFloat

• baselineMap / linkRects / hitTestData (enough for selection + click-to-jump)

• drawPayload: DrawPayload (backend-specific, see below)

• sourceRange: NSRange

  

Indexing

• BlockIndex

• ordered [BlockModel] by range.location

• supports binary search by source location

• stores a prefix-sum height table once blocks are laid out (for scrolling)

  

Pipeline stages (pure functions where possible)

1. Snapshot

  

• Capture DocumentSnapshot with:

• revision: Int

• text: NSString

• lineIndex: LineIndex (newline offsets in UTF-16)

• Snapshot is immutable and safe to process off-main.

  

2. Dirty region expansion

  

• Convert the edit range into a safe reparse region:

• Expand to full lines

• Expand to enclosing fence if inside a code fence

• Expand outward to nearest “hard boundaries” (blank line boundaries, headings, thematic breaks), plus a small safety margin

  

3. Parse

  

• Parse only that region (cmark-gfm or other parser module)

• Produce block candidates with ranges relative to the snapshot

  

4. Block diff

  

• Compare new blocks in region vs old blocks in region

• Preserve BlockIDs for unchanged blocks so caches survive inserts above

• Output:

• removedBlockIDs

• insertedBlocks

• updatedBlocks (same ID, changed content/metadata)

• rangeShiftDeltas (for blocks after the edit)

  

5. Layout

  

• For each inserted/updated block, compute LayoutBlock given:

• width constraint

• theme style set

• font metrics

• Cached by (BlockID, widthBucket, themeVariant, contentHash)

  

6. Render apply

  

• Main thread applies a minimal diff to the preview:

• update block list

• update height prefix sums

• invalidate visible rects for updated blocks only

  

Render backend (two-tier strategy)

  

Backend A: TextKit preview (fast to ship)

• Each block produces an NSAttributedString

• Preview is a read-only NSTextView, but you update it using block-level splices

• You still keep the block model and layout cache so you do not rebuild the world

  

Backend B: Custom CoreText preview (max control)

• A custom NSView inside NSScrollView

• Draws only visible blocks (tiled rendering)

• Each LayoutBlock.drawPayload stores CoreText layout objects (or your own line break results)

• Scroll stays smooth for huge docs because you never ask TextKit to hold the entire preview

  

Design the engine so both backends share the same BlockModel and LayoutBlock types. Switching backends later should touch only the preview module.

  

⸻

  

Concurrency, cancellation, and backpressure

  

This is where “don’t waste your backpressure” becomes a concrete design constraint: the system must generate fast, deterministic feedback and must not do pointless work when the user types quickly.  

  

A single coordinator with bounded work

  

Create a LayoutCoordinator (an actor is perfect in Swift):

  

Responsibilities:

• Accept edit events with (editedRange, delta, revision)

• Coalesce edits into a single dirty region

• Maintain “latest desired revision”

• Run background pipeline jobs

• Drop obsolete work automatically (last-write-wins)

  

Important behavior:

• If a new edit arrives, cancel the in-flight job.

• Never queue unbounded jobs.

• Keep only the newest snapshot and newest dirty region.

  

This avoids the lock-contention and shared-file bottlenecks Cursor saw when agents coordinated via locks.  

  

Two-phase commit to UI

  

Each background job computes PreviewDiff(revision: Int, changes: …).

Main thread applies it only if diff.revision == currentRevision.

  

No partial apply. No stale apply.

  

Explicit “quality backpressure” hooks

  

Make it cheap for agents (and humans) to know when a change is correct:

• scripts/check.sh runs:

• swiftformat (or equivalent)

• swiftlint (if you choose)

• unit tests

• Add a perf smoke test script that loads large fixtures and simulates edits

  

This aligns with:

• Cursor’s emphasis on verifiable goals and tests  

• HumanLayer’s point that you should not use the model as a linter, use deterministic tools and optionally hooks to feed failures back  

• Moss’s point that build tooling feedback prevents you from spending attention on trivial corrections  

  

⸻

  

Repo layout to seed the project (agent-friendly)

  

This structure makes boundaries obvious, keeps context clean, and supports progressive disclosure.

  
```
MarkdownEditor/

  README.md

  

  CLAUDE.md               # or AGENTS.md. Short. Universal. Points elsewhere.

  agent_docs/

    architecture_overview.md

    build_and_test.md

    layout_pipeline.md

    performance_targets.md

    conventions.md

  

  .claude/                # if you are using Cursor agents

    skills/

      project.md

    commands/

      check.md

      test.md

      perf.md

    plans/                 # saved plans and task breakdowns

  

  apps/

    MarkdownApp/           # macOS AppKit app target

      AppDelegate.swift

      EditorViewController.swift

      PreviewViewController.swift

      Assets.xcassets

  

  packages/

    MarkdownEngine/        # Swift Package

      Package.swift

      Sources/

        MarkdownCore/

          DocumentSnapshot.swift

          LineIndex.swift

          DirtyTracker.swift

          BlockID.swift

        MarkdownParse/

          MarkdownParser.swift

          CmarkParser.swift

          BlockBuilder.swift

        MarkdownLayout/

          LayoutEngine.swift

          LayoutCache.swift

          LayoutTypes.swift

        MarkdownRender/

          PreviewDiff.swift

          TextKitPreviewRenderer.swift

          CoreTextPreviewRenderer.swift   # optional later

        MarkdownPerf/

          Signposts.swift

          PerfCounters.swift

      Tests/

        MarkdownCoreTests/

        MarkdownParseTests/

        MarkdownLayoutTests/

        MarkdownPerfTests/

  

  scripts/

    check.sh

    test.sh

    perf_smoke.sh

  fixtures/

    large_1mb.md

    large_10mb.md

    pathological_lists.md

    fenced_blocks.md
```
  

Why this layout works well for agents

  

1) CLAUDE.md stays short

HumanLayer’s guidance is that the root instruction file should be concise and universally applicable, and that you should use progressive disclosure by pointing to deeper docs instead of stuffing everything into one place.  

  

2) agent_docs provides progressive disclosure

Agents can pull in only what they need for a task. This reduces instruction overload and keeps context relevant.  

  

3) .cursor rules and plans mirror how Cursor recommends working

• Start with plans and save them for resumability and future context  

• Use rules for essential commands and workflow, keep them focused  

• Use commands for common multi-step workflows  

  

4) Engine as a Swift Package creates hard module boundaries

Agents can work on MarkdownLayout without accidentally touching UI code, or vice versa. This also reduces merge conflict surface.

  

⸻

  

Agent operating model for building this system

  

If you’re going to build this “agentically”, adopt a light version of the planner/worker/judge pattern Cursor found effective at scale.  

  

Roles

• Planner: writes a plan file in .cursor/plans/ (or agent_docs/plans/) with:

• target files

• acceptance criteria

• tests to add or update

• Worker: implements a single cohesive slice

• Judge: runs scripts/check.sh, reviews diffs, verifies perf counters moved in the right direction

  

This avoids “flat self-coordination” pathologies like lock bottlenecks and risk-averse churning.  

  

Definition of done for any task

  

Every task PR must include at least one of:

• new or updated unit tests

• a perf counter/signpost measurement

• a fixture-based regression case

  

This enforces verifiable goals and creates automated backpressure.  

  

⸻

  

Seed milestone: layout architecture first, UI second

  

To get momentum without painting yourself into a corner, seed in this order:

1. MarkdownCore

  

• DocumentSnapshot, LineIndex, DirtyTracker

• unit tests for range math and line index updates

  

2. MarkdownParse

  

• parser wrapper (cmark-gfm)

• block builder that produces [BlockModel]

• golden tests on fixtures

  

3. MarkdownLayout

  

• LayoutEngine that can compute heights and basic hit-test data for a BlockModel

• cache keyed by (BlockID, widthBucket, theme, contentHash)

  

4. MarkdownRender (TextKit backend)

  

• minimal preview renderer that splices changed blocks only

  

5. App

  

• split view

• editor events feed coordinator

• preview diffs apply only when revision matches

  

If you later decide TextKit preview is not enough, you swap only the renderer backend.

  

⸻

  

What I would put in the root CLAUDE.md / AGENTS.md (shape, not full text)

  

Keep it short and map-like:

• What this repo is (macOS AppKit Markdown editor)

• Where the engine lives (packages/MarkdownEngine)

• The three commands to run (scripts/check.sh, scripts/test.sh, scripts/perf_smoke.sh)

• A pointer list to agent_docs/… files for deeper context

• A single rule: do not block typing; background work must be cancelable

  

This matches the “WHY / WHAT / HOW” onboarding goal and the “less is more” principle.  

  

⸻

  

If you want, I can also propose the exact Swift protocols and the diff format (types and responsibilities) so that multiple agents can implement Parse, Layout, and Render in parallel without stepping on each other.

---

## 20260215_173203

Add a mise.toml and prek.toml for environment and pre-commit

---

## 20260215_174131

Add a SessionStart hook and any other optimization you notice so that claude code web can build this without interruption https://code.claude.com/docs/en/claude-code-on-the-web#cloud-environment

---

## 20260215_180054

Use settings.json instead of settings.local.json

---

## 20260215_180422

It shouldn’t be git ignored this is how all future envs get setup

---

## 20260215_180730

Add a hook so that ExitPlanMode saves the plan to plans/ with a clean timestamp prefix

---

## 20260215_182124

Let’s add another hook to store user prompts on UserPromptSubmit to prompts/

---

## 20260215_182607

Can you back fill that for me based on our current session

---

